QUIZ_DATA_URL=https://quizengine.onrender.com/api/quiz-data
PAYMENTS_MODE=mock

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

FIREBASE_PROJECT_ID=
FIREBASE_SERVICE_ACCOUNT_JSON=

# Firestore REST API fallback (no service account needed — uses the client API key).
# Set these to read quizzes from a different Firebase project than the Admin SDK.
# The quiz must have visibility="public" for unauthenticated REST reads to succeed.
FIREBASE_REST_PROJECT_ID=
FIREBASE_REST_API_KEY=
