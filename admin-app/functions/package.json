{
  "name": "functions",
  "description": "Cloud Functions for quiz-engine",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "migrate:workhub:hr-template": "node lib/backfill-workhub-hr-template.js"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^7.0.6",
    "nodemailer": "^7.0.10"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "typescript": "^5.9.3"
  },
  "private": true
}
